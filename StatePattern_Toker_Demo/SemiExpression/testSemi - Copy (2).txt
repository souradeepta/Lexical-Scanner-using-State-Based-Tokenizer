//This a C Comment

#include<iostream3>

using namespace Testing{

{
  StringBuilder temp = new StringBuilder();
  temp.Append("{");
  temp.Append(String.Format("{0,-10}", type)).Append(" : ");
  temp.Append(String.Format("{0,-10}", name)).Append(" : ");
  temp.Append(String.Format("{0,-5}", beginLine.ToString()));	
  temp.Append(String.Format("{0,-5}", endLine.ToString()));    
  temp.Append("}");
  return temp.ToString();
}
//This is a semi expression test
int i;
/* And a CPP 
Style
Comment
*/ 
double d;
string str1 = 'SampleSingleQuotedString';
string str2 = "SampleDoubleQuotedString";
char ch = '\n';
string s = "\n";

this is a final line with one semiExp; this is some text after that

int p;
for(int i=0; i<4; ++i)
  for(int j=0; j<5; ++j)
    for(int k=0; k<2; ++k)
      p = i*j*k;

